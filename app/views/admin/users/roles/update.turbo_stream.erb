<%# Only the changed row and the flash, so the search and scroll survive. %>
<%= turbo_stream.replace dom_id(@user) do %>
  <%= render "admin/users/user", user: @user %>
<% end %>

<%= turbo_stream.replace "flash" do %>
  <%= render "shared/flash" %>
<% end %>
