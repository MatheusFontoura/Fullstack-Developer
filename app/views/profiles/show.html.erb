<% content_for :title, "Your profile" %>

<div class="mx-auto w-full max-w-2xl">
  <h1 class="text-2xl font-semibold tracking-tight">Your profile</h1>

  <div class="card mt-6">
    <div class="flex items-center gap-4 border-b border-slate-100 p-5">
      <%= render "shared/avatar", user: @user, size: "size-16" %>
      <div class="min-w-0">
        <p class="truncate text-lg font-medium text-slate-900"><%= @user.full_name %></p>
        <p class="truncate text-sm text-slate-500"><%= @user.email %></p>
      </div>
    </div>

    <div class="flex items-center justify-between p-5">
      <dt class="text-sm font-medium text-slate-500">Role</dt>
      <dd><span class="<%= @user.admin? ? "badge-admin" : "badge-user" %>"><%= @user.role.capitalize %></span></dd>
    </div>
  </div>
</div>
